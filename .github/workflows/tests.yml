name: Format Check
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y clang-format-18
      - run: find . -name "*.c" -o -name "*.h" | xargs clang-format-18 --dry-run -Werror
