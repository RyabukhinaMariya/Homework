#ifndef ARRAY_OPERATIONS_H
#define ARRAY_OPERATIONS_H

// Объявления функций для работы с массивом
float* make_new_arr(float* arr, int curr_size);
void print_arr(float* arr, int size_of_arr);
void add(float* array, int count_of_el, float el);

#endif